import React, { useCallback, useState } from 'react';
import { Button, Input, Loadar } from "../index";
import { useSelector } from 'react-redux';
import { useNavigate } from 'react-router-dom';
function PostForm() {
    
    return (
        <div>

        </div>
    );
}

export default PostForm;